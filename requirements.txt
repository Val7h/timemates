fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pg8000==1.31.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
aiofiles==23.2.1
pillow==10.4.0
pywebpush==2.0.0
stripe==11.4.0
slowapi==0.1.9
requests==2.32.3
unicodedata2==15.1.0

# Background scheduler (used by notification_service.py)
apscheduler==3.10.4

# Calendar Integration
google-auth==2.25.2
google-auth-oauthlib==1.1.0

# Sentry error tracking
sentry-sdk[fastapi]==2.18.0

# PostHog product analytics
posthog==3.7.0

# Túnel do Tempo — Face Detection (Sprint 2 S2)
# Chosen opencv-python-headless over face-recognition because face_recognition
# requires dlib, which needs cmake + boost to compile on Linux (~1.5GB build,
# frequently fails on Render free tier). Haar Cascades come pre-trained with
# opencv, no compilation step.
opencv-python-headless==4.8.1.78
numpy==1.26.4

# SendGrid HTTP API (replaces Gmail SMTP after email bombardment crisis)
sendgrid==6.11.0
